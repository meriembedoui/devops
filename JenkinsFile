pipeline {
    agent any

    tools {
        maven 'maven'
        jdk 'jdk17'
    }
    stages {
        stage('Clone repository') {
            steps {
                git branch: 'master',
                    url: 'https://github.com/meriembedoui/devops.git'
            }
        }
        
   }   
}
